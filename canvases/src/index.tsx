import React from 'react';
import ReactDOM from 'react-dom/client';
import PaymentPRDWeb from '../payment-prd-web.canvas';

ReactDOM.createRoot(document.getElementById('root') as HTMLElement).render(
  <React.StrictMode>
    <PaymentPRDWeb />
  </React.StrictMode>,
);
